{
  "schema": "QWEN_BATCH_AUTOTUNE_TRIGGER_V1",
  "enabled": true,
  "candidate_batch_sizes": [8],
  "sample_size": 24,
  "full_sweep_every_hours": 6,
  "scheduled_health_check": "single champion-sized health check; production Hospitality/Tender/GWS throughput has priority over experimental tuning",
  "capacity_policy": {
    "priority": "live discovery and DCE/SUPERGREEN throughput before experimental autotuning",
    "max_configured_parallel_candidates": 1,
    "reason": "Serialize experimental Qwen autotuning to one runner so it cannot consume multiple physical slots while production discovery/classification backlogs are active."
  },
  "policy": {
    "row_conservation_required": true,
    "dce_golden_gate_required": true,
    "singleton_fallbacks_allowed_for_champion": 0,
    "automatic_rejection_enabled": false,
    "failed_batches_recursively_split": true
  }
}
