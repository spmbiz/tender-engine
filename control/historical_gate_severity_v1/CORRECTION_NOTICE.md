# Correction notice — Historical Gate Severity Review v1

**Status: SUPERSEDED / DO NOT USE FOR BUSINESS GATE CLAIMS.**

This review was built from the first historical DCE reality-check bundle. Provenance QA subsequently showed that the French `DOWNLOADED_PUBLIC` files in that bundle were BOAMP publication notice PDFs rather than consultation/DCE packages. The first conservative hard-wording pass also produced some lexical false positives (for example `numéro de référence` / `délai minimum de validité des offres`).

Accordingly, the v1 severity counts are retained only for audit/debugging and must not be used as evidence that a sub-niche requires or does not require references, certifications, insurance, turnover, staffing, onsite work, samples, or other mandatory gates.

Authoritative historical gate research must use downstream consultation-package retrieval after the BOAMP route resolver and the fail-closed `NOTICE_ONLY_NOT_DCE` guard.
