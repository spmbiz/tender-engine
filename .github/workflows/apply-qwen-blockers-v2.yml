name: Apply Qwen Blocker Repair V2 (Legacy Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo 'QWEN_BLOCKER_REPAIR_V2_RETIRED'
          echo 'The audited Qwen V3 control plane is canonical.'
          echo 'This legacy workflow has no write permission and cannot reintroduce direct 480-row dispatchers.'
