name: Qwen Business Fit V2 Migration — Retired

# Historical one-shot migration retained as documentation only.
# The calibrated business-fit logic is already in the production pipeline.
# This workflow must never rewrite main, cancel Qwen, or restart the live classifier.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired migration: no-op.'
          echo 'Use the production Qwen workflow and regression harness instead.'
