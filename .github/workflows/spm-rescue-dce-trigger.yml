name: SPM Rescue DCE Trigger (Legacy Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy rescue dispatcher retired.'
          echo 'Reason: it hard-coded an obsolete source_discovery_run and duplicated DCE Queue Drainer dispatch.'
          echo 'Authoritative path: queues/spm_rescue_selection_latest.jsonl -> DCE Queue Drainer -> exact source run + anti-repeat + shared fair-share admission.'
