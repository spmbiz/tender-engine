name: Qwen Emergency Cancel Migration — Retired

# Historical emergency helper retained as a no-op only.
# Stale-run recovery is owned by the stage-aware production watchdog.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired emergency migration: no-op.'
          echo 'The stage-aware watchdog is the only automatic stale-run canceller.'
