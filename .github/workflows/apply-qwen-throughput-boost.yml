name: Apply Qwen Throughput Boost (Legacy Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo 'QWEN_THROUGHPUT_BOOST_RETIRED'
          echo 'The audited Qwen V3 control plane is canonical.'
          echo 'This legacy workflow cannot restore hourly 480-row scheduling or stale recovery writers.'
