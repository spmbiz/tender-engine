name: Greece KIMDIS 404 Contract Probe

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/gr-khmdhs-404-probe.yml'
      - 'pipeline/probe_gr_khmdhs_404.py'
      - 'control/gr_khmdhs_404_probe_trigger.txt'

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with: {ref: main, fetch-depth: 1, fetch-tags: false}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check -q requests
      - name: Capture exact 404 and adjacent controls
        shell: bash
        run: |
          set +e
          python pipeline/probe_gr_khmdhs_404.py
          rc=$?
          echo "probe_rc=$rc"
          exit 0
      - name: Persist probe
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          test -f control/gr_khmdhs_404_probe.json || printf '%s\n' '{"schema":"GR_KHMDHS_404_PROBE_WORKFLOW_FAILURE","pass":false}' > control/gr_khmdhs_404_probe.json
          git config user.name tender-engine-bot
          git config user.email tender-engine-bot@users.noreply.github.com
          git add control/gr_khmdhs_404_probe.json
          git diff --cached --quiet && exit 0
          git commit -m "diag: persist Greece KIMDIS 404 contract ${GITHUB_RUN_ID}"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push origin HEAD:main && exit 0
            git rebase --abort >/dev/null 2>&1 || true
            sleep $((i*2))
          done
          exit 1
