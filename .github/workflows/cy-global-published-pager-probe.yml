name: Cyprus Global Published Notices Pager Proof

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cy-global-published-pager-probe.yml'
      - 'pipeline/probe_cy_global_published_pager.py'
      - 'control/cy_global_published_pager_trigger.txt'

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with: {ref: main, fetch-depth: 1, fetch-tags: false}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check -q requests beautifulsoup4
      - name: Follow publisher-provided national pager links
        shell: bash
        run: |
          set +e
          python pipeline/probe_cy_global_published_pager.py
          rc=$?
          echo "probe_rc=$rc"
          exit 0
      - name: Persist pager proof
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          test -f control/cy_global_published_pager_probe.json || printf '%s\n' '{"schema":"CY_EPPS_GLOBAL_PUBLISHED_PAGER_WORKFLOW_FAILURE","pass":false}' > control/cy_global_published_pager_probe.json
          git config user.name tender-engine-bot
          git config user.email tender-engine-bot@users.noreply.github.com
          git add control/cy_global_published_pager_probe.json
          git diff --cached --quiet && exit 0
          git commit -m "diag: persist Cyprus global Published pager ${GITHUB_RUN_ID}"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push origin HEAD:main && exit 0
            git rebase --abort >/dev/null 2>&1 || true
            sleep $((i*2))
          done
          exit 1
