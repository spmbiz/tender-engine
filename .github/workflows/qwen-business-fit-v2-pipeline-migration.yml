name: Qwen Business Fit V2 Pipeline Migration — Retired

# Historical one-shot migration retained as documentation only.
# Production already carries the calibrated business-fit pipeline.
# Never rewrite main or restart/cancel Qwen from this retired workflow.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired migration: no-op.'
          echo 'Use the production Qwen workflow and A/B regression harness instead.'
