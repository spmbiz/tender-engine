name: Qwen Preempt Stale Run (Legacy Disabled)

# Retired emergency one-shot for historical run 31922632641. The freshness
# watchdog now owns stale-run recovery with stage-aware age limits.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo 'QWEN_PREEMPT_STALE_RETIRED'
          echo 'No Qwen run is cancelled or dispatched from this legacy workflow.'
