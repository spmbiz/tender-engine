name: Pipeline A/B Live Canary V1 — Retired

# Retired because historical golden tender deadlines made the canary time-sensitive.
# V2 materializes one byte-identical future-dated A/B fixture for rollback/current.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired V1 canary: no-op.'
          echo 'Use pipeline-ab-live-canary-v2.yml.'
