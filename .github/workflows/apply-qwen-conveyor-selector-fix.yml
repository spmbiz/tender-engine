name: Apply Qwen Conveyor Selector Fix (Legacy Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo 'QWEN_CONVEYOR_SELECTOR_FIX_RETIRED'
          echo 'The audited Qwen V3 control plane is canonical.'
          echo 'This legacy workflow cannot restore direct semantic dispatch or 480-row conveyor behavior.'
