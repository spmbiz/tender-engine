name: Apply Qwen Final Hardening (Legacy Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo 'QWEN_FINAL_HARDENING_RETIRED'
          echo 'The audited Qwen V3 control plane is canonical.'
          echo 'This legacy workflow cannot rewrite semantic scheduling or restore 480-row settings.'
