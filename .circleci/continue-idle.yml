version: 2.1
jobs:
  idle:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - run: echo "CircleCI fleet idle; no intentional tick in this commit."
workflows:
  idle-only:
    jobs:
      - idle
