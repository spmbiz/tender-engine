version: 2.1
jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Diagnostic hello
          command: |
            echo 'CIRCLE_DIAGNOSTIC_HELLO_PASS'
            getconf _NPROCESSORS_ONLN || true
workflows:
  diagnostic:
    jobs:
      - say-hello
